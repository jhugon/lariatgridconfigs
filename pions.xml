<?xml version="1.0"?>

<!-- Production Project -->
<!-- for v2 just changed the config file in project, not in the tarball. lets see if it changes: Oops, forgot to rebuild. -->
<!-- for v3 just changed the config file in project, and rebuild, not in the tarball. lets see if it changes: Yes!-->
<!-- for v4, increase to 10000 total -->
<!-- for v5, fixed outnames to use jobid in fcl -->
<!-- for v6, fixed outnames to use run, subrun in fcl and 2 jobs 100/evts/job-->
<!-- for v7, increase to 50000 total-->
<!-- for v8, Use RecoMC.fcl. Only 5000 events and 10jobs-->

<!DOCTYPE project [
<!ENTITY release "v01_12_01">
<!ENTITY file_type "mc">
<!ENTITY run_type "physics">
<!ENTITY name "lariat_PiAbsAndChEx_flat_pip_v8">
<!ENTITY tag "TestOfBatchJobs">
]>

<project name="&name;">

  <!-- Project size -->
  <numevents>5000</numevents>
  <!-- Try to do 500 events/job  ~2.5hr for reco -->

  <!-- Operating System -->
  <os>SL6</os>

  <!-- Batch resources -->
  <resource>DEDICATED,OPPORTUNISTIC</resource>

  <!-- Larsoft information -->
  <larsoft>
      <tag>&release;</tag>
      <qual>e7:prof</qual>
      <local>/lariat/app/users/jhugon/genPionAbsAndChEx/lariatsoft_jhugon_PionAbsChEx_abc59058.tar</local>
  </larsoft>

  <!-- Project stages -->

  <stage name="gensimdigi">
      <fcl>prodsingle_lariat_PiAbsAndChEx_pip.fcl</fcl>
      <outdir>/pnfs/lariat/persistent/users/jhugon/&release;/gensimdigi/&name;</outdir>
      <workdir>/lariat/app/users/jhugon/lariatOffline/&release;/gensimdigi/&name;</workdir>
      <numjobs>10</numjobs>
      <datatier>simulated</datatier>
  </stage>

  <stage name="reco">
      <inputlist>/lariat/app/users/jhugon/genPionAbsAndChEx/pip_v7_genfiles_10k.txt</inputlist>
      <fcl>RecoMC.fcl</fcl>
      <outdir>/pnfs/lariat/persistent/users/jhugon/&release;/reco/&name;</outdir>
      <workdir>/lariat/app/users/jhugon/lariatOffline/&release;/reco/&name;</workdir>
      <numjobs>10</numjobs>
      <datatier>reconstructed-2d</datatier>
  </stage>

  <!-- file type -->
  <filetype>&file_type;</filetype>

  <!-- run type -->
  <runtype>&run_type;</runtype>

</project>
